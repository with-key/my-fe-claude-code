# 테스트 시나리오: {{기능명}}

> 원본 문서: {{원본 경로}}
> 생성일: {{날짜}}

## 개요

{{기능 요약}}

---

## 테스트 시나리오

### 1. 정상 동작 (Happy Path)

#### Scenario: {{시나리오 제목}}

```gherkin
Given {{사전 조건}}
When  {{사용자 행동}}
Then  {{기대 결과}}
```

**테스트 데이터:**
| 입력 | 기대 결과 |
|------|-----------|
| {{값}} | {{결과}} |

---

### 2. 경계값 (Boundary)

#### Scenario: {{경계값 시나리오 제목}}

```gherkin
Given {{사전 조건}}
When  {{경계 입력}}
Then  {{기대 결과}}
```

**경계값 테이블:**
| 경계 | 입력값 | 기대 동작 |
|------|--------|-----------|
| 최솟값 | {{min}} | {{동작}} |
| 최솟값 - 1 | {{min-1}} | {{에러}} |
| 최댓값 | {{max}} | {{동작}} |
| 최댓값 + 1 | {{max+1}} | {{에러}} |

---

### 3. 에러 케이스 (Error)

#### Scenario: {{에러 시나리오 제목}}

```gherkin
Given {{사전 조건}}
When  {{잘못된 입력}}
Then  {{에러 처리}}
And   {{에러 메시지}}
```

**에러 케이스 목록:**
| 입력 유형 | 입력값 | 에러 메시지 |
|-----------|--------|-------------|
| 빈 값 | `""` | {{메시지}} |
| null | `null` | {{메시지}} |
| 타입 불일치 | {{값}} | {{메시지}} |

---

### 4. 엣지 케이스 (Edge)

#### Scenario: {{엣지 시나리오 제목}}

```gherkin
Given {{특수 조건}}
When  {{특수 행동}}
Then  {{기대 결과}}
```

---

## 테스트 우선순위

| 우선순위 | 시나리오 | 이유 |
|----------|----------|------|
| P0 (필수) | {{시나리오}} | {{이유}} |
| P1 (중요) | {{시나리오}} | {{이유}} |
| P2 (권장) | {{시나리오}} | {{이유}} |
